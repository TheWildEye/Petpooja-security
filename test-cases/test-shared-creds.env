# Shared Credentials Test Case — CERT-IN Violation (Prohibited)
# This file contains INTENTIONAL compliance violations for testing.

# VIOLATION: Shared service account credentials
# CERT-IN explicitly prohibits shared/generic accounts

# Shared team credentials
TEAM_DB_USER=shared_admin
TEAM_DB_PASSWORD=TeamPassword2024!

# Generic service accounts
SERVICE_ACCOUNT_USER=service_user
SERVICE_ACCOUNT_PASSWORD=ServiceP@ss123

# Shared API access
SHARED_API_KEY=shared-key-for-all-devs-abc123def456
TEAM_ACCESS_TOKEN=team-token-everyone-uses-this

# Shared cloud credentials
# Real format: AKIA[0-9A-Z]{16} — pattern used below is intentionally fake for testing
AWS_SHARED_ACCESS_KEY=FAKE_TEAM_SHARED_KEY_EXAMPLE
AWS_SHARED_SECRET_KEY=SharedSecretKeyForAllTeamMembers_FAKE_EXAMPLE

# Shared admin panel
ADMIN_USER=admin
ADMIN_PASSWORD=admin123

# Shared monitoring
GRAFANA_USER=team_monitor
GRAFANA_PASSWORD=MonitorPass2024

# VIOLATION: Default credentials not changed
REDIS_PASSWORD=
MYSQL_ROOT_PASSWORD=root
POSTGRES_PASSWORD=postgres
MONGO_INITDB_ROOT_PASSWORD=changeme

# SAFE EXAMPLE:
# Each developer/service should have individual credentials
# Use IAM roles, service principals, or individual API keys
# Never use shared passwords across team members
